/**
 * Atlas Rich Input · canonical types (mobile re-export from canon).
 *
 * Implementation lives in `packages/atlas-rich-input-canon` — the single
 * source of truth shared by mobile + desktop + future Atlas surfaces. This
 * thin re-export keeps existing mobile import paths stable
 * (`from './types'`).
 */
export * from '@atlas/rich-input-canon'
